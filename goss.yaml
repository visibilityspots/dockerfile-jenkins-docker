package:
  docker-ce:
    installed: true

command:
  docker --version:
    exit-status: 0

group:
  docker:
    exists: true
    gid: 900

user:
  jenkins:
    exists: true
    groups:
    - jenkins
    - docker
